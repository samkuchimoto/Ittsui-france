<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              App Links — verified against public/.well-known/assetlinks.json
              (autoVerify triggers Android to check that file at install
              time). Both apex and www hosts are listed because no
              canonical redirect between them was found in the Next.js app
              (capacitor.config.ts's server.url uses the apex domain, but
              the production URL is otherwise referred to as www) — worth
              confirming which is actually canonical and dropping the other
              once that's settled.

              This intent-filter alone only covers installs signed with the
              upload key in assetlinks.json. Once this app is uploaded to
              Play Console with Play App Signing (Google's default), Google
              re-signs it for distribution — that certificate's fingerprint
              (Play Console → Setup → App signing) must be added to
              assetlinks.json's sha256_cert_fingerprints array too, or
              App Links will fail verification for anyone who installed via
              Play Store specifically.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="ittsui.fr" />
                <data android:scheme="https" android:host="www.ittsui.fr" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
