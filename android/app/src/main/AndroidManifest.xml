<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              App Links — verified against public/.well-known/assetlinks.json
              (autoVerify triggers Android to check that file at install
              time). Both apex and www hosts are listed because no
              canonical redirect between them was found in the Next.js app
              (capacitor.config.ts's server.url uses the apex domain, but
              the production URL is otherwise referred to as www) — worth
              confirming which is actually canonical and dropping the other
              once that's settled.

              This intent-filter alone only covers installs signed with the
              upload key in assetlinks.json. Once this app is uploaded to
              Play Console with Play App Signing (Google's default), Google
              re-signs it for distribution — that certificate's fingerprint
              (Play Console → Setup → App signing) must be added to
              assetlinks.json's sha256_cert_fingerprints array too, or
              App Links will fail verification for anyone who installed via
              Play Store specifically.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="ittsui.fr" />
                <data android:scheme="https" android:host="www.ittsui.fr" />
            </intent-filter>

            <!--
              @capgo/capacitor-share-target (lib/nativeShareTarget.ts) —
              makes "Ittsui" appear in Android's own Share sheet from any
              other app (TikTok, Instagram, a group chat...), so proposing
              a meetup can start from wherever the person already is
              instead of requiring them to open Ittsui first. text/* only:
              this app has nowhere to put a shared image/video (see that
              file's own comment on why iOS can't get the same treatment
              without a real Xcode Share Extension target).
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- @capacitor-community/contacts' native picker (lib/nativeContacts.ts)
         — Ittsui only ever reads a single contact someone explicitly picks,
         never writes to their device address book. WRITE_CONTACTS is
         declared anyway: verified directly in the plugin's own Android
         source (ContactsPlugin.java) that both permissions share one
         Capacitor permission alias ("contacts"), and the plugin checks
         that whole alias's granted state before allowing pickContact to
         proceed — declaring READ alone left the alias unable to ever
         resolve as fully granted. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <!-- @capacitor-community/speech-recognition (lib/nativeSpeech.ts) — voice
         name lookup in "Mode simple". The plugin's own AndroidManifest.xml
         already declares this (Android's manifest merger would pull it in
         either way), but it's restated here explicitly, same as every other
         native permission in this file, so anyone reading this manifest
         alone sees the full list this app actually depends on. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
</manifest>
